import * as React from 'react';

class DbUsersLineGraph extends React.Component {
  render() {
    const { route, client } = this.props;
    return (
      <div>

      </div>
    );
  };
}

export default DbUsersLineGraph;
