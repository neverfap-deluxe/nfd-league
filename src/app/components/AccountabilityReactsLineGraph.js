import * as React from 'react';

class AccountabilityReactsLineGraph extends React.Component {
  render() {
    const { route, client } = this.props;
    return (
      <div>


      </div>
    );
  };
}

export default AccountabilityReactsLineGraph;
